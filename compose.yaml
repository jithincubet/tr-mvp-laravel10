services:
    laravel.test:
        build:
            context: './vendor/laravel/sail/runtimes/8.5'
            dockerfile: Dockerfile
            args:
                WWWGROUP: '${WWWGROUP}'
        image: 'sail-8.5/app'
        extra_hosts:
            - 'host.docker.internal:host-gateway'
        ports:
            - '8080:80'
            - '${VITE_PORT:-5173}:${VITE_PORT:-5173}'
        environment:
            WWWUSER: '${WWWUSER}'
            LARAVEL_SAIL: 1
            XDEBUG_MODE: '${SAIL_XDEBUG_MODE:-off}'
            XDEBUG_CONFIG: '${SAIL_XDEBUG_CONFIG:-client_host=host.docker.internal}'
            IGNITION_LOCAL_SITES_PATH: '${PWD}'
        volumes:
            - '.:/var/www/html'
        networks:
            - sail
            - inner-network
    #     depends_on:
    #         - mysql
    # mysql:
    #     image: 'mysql:8.4'
    #     container_name: sail-sc-mysql
    #     restart: always
    #     tty: true
    #     environment:
    #         MYSQL_DATABASE: dev
    #         MYSQL_PASSWORD: dev
    #         MYSQL_ROOT_PASSWORD: dev
    #         MYSQL_USER: dev
    #     ports:
    #         - '3306:3306'
    #     expose:
    #         - '3306'
    #     volumes:
    #         - 'sail-mysql:/var/lib/mysql'
    #         - './vendor/laravel/sail/database/mysql/create-testing-database.sh:/docker-entrypoint-initdb.d/10-create-testing-database.sh'
    #     networks:
    #         - sail
    #     healthcheck:
    #         test:
    #             - CMD
    #             - mysqladmin
    #             - ping
    #             - '-p${DB_PASSWORD}'
    #         retries: 3
    #         timeout: 5s
networks:
    sail:
        driver: bridge
    inner-network:
        external: true
        name: docker_inner-network
volumes:
    sail-mysql:
        driver: local
